server {
    listen 80;

    location /api/ {
        proxy_pass http://api-service:5000;
    }

    location / {
        return 200 'Library platform is running';
        add_header Content-Type text/plain;
    }
}